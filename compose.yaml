services:
  brawl-claimer:
    build: .
    restart: unless-stopped
    cpus: 0.5
    mem_limit: 768m
    shm_size: 512mb
    env_file:
      - .env.local
    ports:
      - "3100:3100"
    volumes:
      - brawl-claimer-state:/app/state

volumes:
  brawl-claimer-state:
