services:
  brawl-stars-claimer:
    build: .
    restart: unless-stopped
    cpus: 0.5
    mem_limit: 768m
    shm_size: 512mb
    env_file:
      - .env.local
    ports:
      - "3100:3100"
    volumes:
      - brawl-stars-claimer-state:/app/state

volumes:
  brawl-stars-claimer-state:
